# Earth Engine integration

The prototype plan has been superseded by the implemented [run/import handoff](gee-analysis.md), [methodology](methodology.md) and [data contract](data-schema.md).

Do not use the old fixed canopy threshold, 2016 surface-reflectance assumptions, illustrative import figures or derived 0–100 score. Production data is pending until actual exports are calibrated and reviewed. The original v0.1 starter remains in `files/` and `data/boundaries/` for provenance only.
