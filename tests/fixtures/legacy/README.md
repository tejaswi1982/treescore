# Legacy fixtures — NOT REAL DATA

These non-executable text files preserve the original prototype's illustrative metrics. Their old `gee-export` metadata was incorrect. They must never be imported, interpreted as real satellite output or deployed as measured results. Active tests construct their own explicitly synthetic observations in memory.
